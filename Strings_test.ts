import { assertEquals } from "https://deno.land/std/assert/mod.ts";


Deno.test("Head returns the first word in a string", async () => {
    assertEquals(true, false);
});

Deno.test("Tail returns all after the first word in a string", async () => {
    assertEquals(true, false);
});

Deno.test("Before returns the part of a string before a divider", async () => {
    assertEquals(true, false);
});

Deno.test("After returns the part of a string after a divider", async () => {
    assertEquals(true, false);
});

Deno.test("Replace all replaces every instance of the keys in the template with values", async () => {
    assertEquals(true, false);
});
